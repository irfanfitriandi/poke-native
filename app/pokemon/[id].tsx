import { SafeAreaView } from 'react-native-safe-area-context'

import MonoText from '@/components/shared/MonoText'

export default function PokemonDetail() {
  return (
    <SafeAreaView>
      <MonoText>PokemonDetail</MonoText>
    </SafeAreaView>
  )
}
